#load "core/std"

use core
use cbindgen

main :: () {
    path := module_path(#file);

    result := compile_c_file(string.concat(path, "stb_image.c"), string.concat(path, "stb_image"));
    if !result {
        return os.exit(1);
    }
}
